# https://console.anthropic.com/settings/keys - only needed for `optimize`,
# not for `compare-price` (that command never calls Claude).
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-5

# Where this project's SQLite store (interaction logs) lives.
CORE_DB_PATH=./data/listing-optimizer.sqlite
