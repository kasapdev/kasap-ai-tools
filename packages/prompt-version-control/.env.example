# prompt-version-control tamamen yerel çalışır - Anthropic API anahtarı ya da
# başka bir sağlayıcıya ait kimlik bilgisi gerektirmez (kasıtlı olarak
# sağlayıcıdan bağımsızdır).

# Bu projenin kendi SQLite deposunun (versiyon geçmişi) yaşadığı yer.
# Opsiyonel - verilmezse ./data/prompt-version-control.sqlite kullanılır.
PROMPT_VC_DB_PATH=./data/prompt-version-control.sqlite
