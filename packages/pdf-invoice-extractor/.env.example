# https://console.anthropic.com/settings/keys - required for `extract`, which
# calls Claude to turn the extracted PDF text into structured invoice fields.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-5

# Where this project's SQLite store (interaction logs) lives.
CORE_DB_PATH=./data/pdf-invoice-extractor.sqlite
