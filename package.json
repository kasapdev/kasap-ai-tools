{
  "name": "kasap-ai-tools",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "test": "pnpm -r --if-present run test",
    "typecheck": "pnpm -r --if-present run typecheck",
    "dev:berilis": "pnpm --filter @kasap/berilis-agent dev"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.7.3",
    "vitest": "^4.1.11"
  }
}
